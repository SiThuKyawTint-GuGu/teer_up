export enum WIDTH_TYPES {
  FULL = "FULL",
  HALF = "HALF",
}

export enum HEIGHT_TYPES {
  SCREEN = "SCREEN",
  FULL = "FULL",
  HALF = "HALF",
}

export const bgTypes = {
  [WIDTH_TYPES.FULL]: "100%",
  [WIDTH_TYPES.HALF]: "50%",
};
