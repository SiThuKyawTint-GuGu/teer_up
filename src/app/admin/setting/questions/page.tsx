import Questions from "@/page-containers/admin/questions";

const QuestionPage = () => {
  return (
    <div className="pt-2 px-2" style={{ maxHeight: "calc(100vh - 200px)" }}>
      <Questions />
    </div>
  );
};

export default QuestionPage;
